# Backend port. Production frontend is served by this same process.
PORT=3001
# Only needed for a separately hosted frontend. Comma separated exact origins.
# ALLOWED_ORIGINS=https://your-frontend.example
# Build-time Vite variable; omit for same-origin hosting (recommended).
# VITE_SERVER_URL=https://your-backend.example
